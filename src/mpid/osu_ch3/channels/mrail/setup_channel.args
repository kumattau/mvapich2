for arg in $ac_configure_args ; do
  case $arg in
    \'--with-rdma=*)
    rdma_name=`echo $arg | sed 's/--with-rdma=//'`
    rdma_name=`echo $rdma_name | sed "s/'//g"`
    ;;
  esac
done

if test -z "$rdma_name" ; then
  echo Error: --with-rdma not specified.
  exit 1
fi
